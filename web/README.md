# Web dashboard (v1.1 Stage 2)

Next.js App Router scaffold. It reads a static snapshot from `public/data/`.
Interactive charts are not implemented yet. Vercel is not configured yet.

```bash
cd web
npm install
npm run dev
npm run lint
npm run typecheck
npm run build
```
